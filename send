[[ "$(hostname)" == "cb3" ]] && {
  cd frontend
  yes|npx npm-check-updates -u --filter vite --pre 1 && yes|npx npm-check-updates -u --reject vite
  cd ..
  git add .
  git commit -m .
  git push
  ssh p72-z "cd code/nixium;rm frontend/pnpm-lock.yaml;git clean -fd;git pull"  
}
